import { useState, type FormEvent } from 'react'
import { useNavigate } from 'react-router'
import { useAuth } from '../../context/AuthContext'
import { supabase } from '../../services/supabaseClient'

export default function ChangePasswordPage(){const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const {refreshUser}=useAuth();const navigate=useNavigate();const submit=async(e:FormEvent)=>{e.preventDefault();if(password.length<8||password!==confirm){setError(password.length<8?'La contraseña debe tener al menos 8 caracteres.':'Las contraseñas no coinciden.');return}setBusy(true);const {error:updateError}=await supabase.auth.updateUser({password});if(!updateError){const {error:rpcError}=await supabase.rpc('complete_required_password_change');if(!rpcError){await refreshUser();navigate('/app/dashboard',{replace:true});return}setError(rpcError.message)}else setError(updateError.message);setBusy(false)};return <main className="grid min-h-screen place-items-center bg-[var(--background)] p-5"><form onSubmit={(e)=>void submit(e)} className="w-full max-w-md space-y-4 rounded-3xl border border-[var(--border-soft)] bg-[var(--surface)] p-7"><h1 className="text-2xl font-semibold">Cambia tu contraseña</h1><p className="text-sm text-[var(--text-secondary)]">Debes crear una contraseña personal antes de continuar.</p><label className="block text-sm">Nueva contraseña<input required minLength={8} type="password" value={password} onChange={(e)=>setPassword(e.target.value)} className="mt-1 w-full rounded-xl border border-[var(--border)] bg-transparent px-3 py-2.5"/></label><label className="block text-sm">Confirmar contraseña<input required minLength={8} type="password" value={confirm} onChange={(e)=>setConfirm(e.target.value)} className="mt-1 w-full rounded-xl border border-[var(--border)] bg-transparent px-3 py-2.5"/></label>{error&&<p className="text-sm text-rose-500">{error}</p>}<button disabled={busy} className="w-full rounded-xl bg-[var(--accent)] px-4 py-3 font-medium text-white disabled:opacity-60">{busy?'Guardando...':'Guardar contraseña'}</button></form></main>}
